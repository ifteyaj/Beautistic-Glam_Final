# Debug Changelog — 20260414_221801_900965

## Changes Made

### 1. Added fallback logic for empty Top Sellers / New Arrivals sections
- **File:** `pages/Home.tsx`
- **Change:** When no products have `isTopSeller`/`isNew` flags set, fall back to showing the 4 highest-rated products for Top Sellers and 4 most recently created products for New Arrivals.
- **Lines:** 7–17 (replaced simple filter with fallback logic)
- **Why:** The Supabase products don't have `isTopSeller`/`isNew` boolean flags, so `products.filter(p => p.isTopSeller)` returned an empty array, leaving both product grids empty.
- **Revert:** Replace the fallback logic with the original two lines:
  ```tsx
  const topSellers = products.filter(p => p.isTopSeller);
  const newArrivals = products.filter(p => p.isNew);
  ```

## Revert Status
- [x] Change 1 — Fallback product sections (permanent fix)
